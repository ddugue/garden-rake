require '../lib/rake/garden'
include Rake::Garden

task :booty do
  puts "BOOTY"
end

chore :test => ["copy/*.txt"] do |t|
  puts "Testing chore"
  t.files.each do |f|
    b = 2
    puts f if t.has_changed f
    cp f, "sub/%f.2.txt"
    sh "sleep 5 && echo 'DAN'"
  end
end
